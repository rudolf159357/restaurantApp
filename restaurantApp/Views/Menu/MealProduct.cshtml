@model Meal

@{
    ViewData["Title"] = "MealProduct";
}

<div class="products">
    <div class="box">
    <h1>@Model.Type</h1>
    <p>Price: <small>@Model.Price</small>$</p>
    <button class="btn btn-dark btn-lg"> <a asp-controller="Menu" asp-action="MealThank" asp-route-id=@Model.Id>Buy</a></button>
    </div>
</div>

